org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
# Jetifier can OOM in CI when transforming Flutter engine artifacts; this plugin is AndroidX-only.
android.enableJetifier=false
